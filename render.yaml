services:
  # Backend API
  - type: web
    name: elyndor-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: free
    region: ohio
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: elyndor-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          type: web
          name: elyndor-frontend
          property: host

  # Frontend Static Site
  - type: web
    name: elyndor-frontend
    runtime: static
    staticPublishPath: frontend/dist
    buildCommand: npm install && npm run build
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: elyndor-backend
          property: host

databases:
  - name: elyndor-db
    plan: free
    region: ohio
